package cz.zcu.kiv.crce.classmodel.definition;

public enum ArgDefinitionType {
    POST, GET, PUT, PATCH, DELETE, HEAD, OPTIONS, TRACE, PATH, EXPECT, SKIP, PARAM, BASEURL, SEND, EXCHANGE, HTTPMETHOD
}
